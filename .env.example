# Point to your FastAPI backend (defaults to localhost:8000)
VITE_API_URL=http://localhost:8000

# If you are using the bundled FastAPI backend with Firebase, configure
# Firebase credentials for the backend in backend/.env.example or your
# deployment environment (FIREBASE_SERVICE_ACCOUNT_PATH or FIREBASE_SERVICE_ACCOUNT_JSON).
